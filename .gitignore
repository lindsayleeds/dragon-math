# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env.local
.env

# Nothing here writes a local database anymore, but restoring an old backup
# (see scripts/migrate-sqlite-to-postgres.cjs) drops one at the repo root —
# a restored database of children's records must never become committable.
*.db

# Playwright tooling
.playwright-cli/
.playwright-mcp/

# Root-level screenshot artifacts
/*.png
